{#
/**
 * @file
 * OVERRIDE default theme implementation to display a formatted image field.
 *
 * Available variables:
 * - image: A collection of image data.
 * - image_style: An optional image style.
 * - url: An optional URL the image can be linked to.
 * - attributes: an associative array of attributes on the link.
 *
 * @see template_preprocess_image_formatter()
 *
 * @ingroup themeable
 */
#}
{% if url %}
  <a href="{{ url }}"{{ attributes }}>{{ image }}</a>
{% else %}
  {{ image }}
{% endif %}
